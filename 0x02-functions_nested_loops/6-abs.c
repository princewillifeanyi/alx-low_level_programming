#include "main.h"
/**
 * _abs - Compute absolute value.
 *@n: Character to convert
 *
 * Return: returns absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
